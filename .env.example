# Not required yet — the core loop scaffold runs on local seed data.
# Fill these in when we wire up Supabase (accounts, cross-device streaks/progress, live question bank).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
