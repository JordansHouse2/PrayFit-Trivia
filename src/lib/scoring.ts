/** Per the product spec: no speed component, ever — depth and participation are rewarded instead. */
export const SCORING = {
  BASE_POINTS_CORRECT: 10,
  DISCERNMENT_BONUS: 5,
  PROGRESS_UNITS_TOUCHED: 1,
  PROGRESS_UNITS_READ: 4,
} as const;
